{
  "name": "sms-notify",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "prettier": "2.2.1",
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
